package com.decisive.sort;

/**
 * 冒泡排序
 */
public class BubblingSort {

    public <T extends Comparable<? super T>> void sort(T[] ts) {
        //检查参数是否合法
        if (ts == null || ts.length < 1) {
            return;
        }

        for (int i = 0; i < ts.length; i++) {

        }

    }

}
